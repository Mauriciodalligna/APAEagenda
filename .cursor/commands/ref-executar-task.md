# Executar task — PRD de refatoração (APAE Agenda)

Segue **integralmente** o fluxo em `.cursor/skills/ref-executar-task/SKILL.md`.

- Pasta: `tasks/prd-refatoracao-[nome-modulo]/`.
- Próxima tarefa: `tasks.md` e `[num]_task.md` — primeira pendente.
- Fluxo: configuração pré-tarefa → análise → resumo → plano → implementação. Use **npm** e o `package.json`.
